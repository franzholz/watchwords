plugin.tx_watchwords_pi1 {
      # cat=plugin.watchwords/file; type=file[html,htm,tmpl,txt]; label= Template File (biblegateway.com): This is a simple textfile where the layout of the watchwords display is defined with HTML-code. Have a look a at EXT:watchwords/pi1/template.inc for a working example.
  file.templateFileBiblegateway = EXT:watchwords/pi1/template_biblegateway.inc
}
