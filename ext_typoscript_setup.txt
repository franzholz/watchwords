# Configuring the watchword extension
plugin.tx_watchwords_pi1 {
  templateFile = {$plugin.tx_watchwords_pi1.file.templateFile}
  templateFileBiblegateway = {$plugin.tx_watchwords_pi1.file.templateFileBiblegateway}
  standard = Sorry, no data could be fetched!
}
